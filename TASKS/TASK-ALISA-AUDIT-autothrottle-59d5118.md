# TASK-ALISA-AUDIT-autothrottle-59d5118

## Роль
Вы — Alisa, независимый внешний аудитор. Режим: **regression-sentinel** — вы не ищете новые баги с нуля, вы проверяете, что Wave 1 (PR #5, смержен в `master`, merge commit `f1b47f98131d85d5fd29683dc0354b46952d4ab2`) не сломал что-то работавшее и не оставил тестовые слепые зоны.

## Цель аудита
Модуль: **`modules/autothrottle.py`** — следующий в очереди модуль Package A, затронутый Wave 1 в обоих раундах (round 1 FIX-08/LITE-008 — units mismatch веса/ВПП; round 2 REM-02 — production-path тест).

Включайте в скоп аудита также точки интеграции:
- `main.py` — метод `_calculate_approach_speeds` (читает `telemetry.get('weight', {})`, конвертирует `total_weight` из фунтов в килограммы и вызывает логику autothrottle для расчёта approach speeds).
- `tests/test_autothrottle.py` — полностью, в т.ч. новые тесты round 2 (`test_calculate_approach_speeds_kwargs`, `test_calculate_approach_speeds_fallback_weight`).

**Не входят в скоп:** safety_guard.py (аудит завершён), navigation.py, virtual_joystick.py, pyproject.toml — это другие модули, будут отдельные задачи.

## Ваш доступ и ограничения
- Репозиторий: `zhuk-mou-1/msfs_autoland`, публичный, текущая `master`. Файлы доступны без авторизации через GitHub API / raw.githubusercontent.com.
- **Важно**: аудитируйте текущий `master` (уже содержит merge коммит `f1b47f98131d85d5fd29683dc0354b46952d4ab2`), а не бейзлайн `3971ba1`. Обязательно зафиксируйте SHA, который вы реально аудитировали, в отчёте.
- Запрет: не запускать код (нет симулятора/привязки). Аудит — только static/manual code review + анализ тестов. Статус каждой находки: STATIC_CONFIRMED / STATIC_SUSPECTED.
- Запрет: не верьте на слово заявления в commit message/PR description — сверяйте с реальным кодом. В прошлый раз в аудите safety_guard была найдена ошибка в счёте тестов и гипотетический канал влияния, который не подтвердился кодом — каждую гипотезу о «влиянии на другой код» доказывайте точной трассировкой передачи значения через конкретные вызовы/аргументы, а не предполагайте её.

## Самые изменения Wave 1 в этом модуле (для контекста, не как готовый вердикт)
- Round 1 (FIX-08, LITE-008): в `main.py._calculate_approach_speeds` вес самолёта (`weight_data.get('total_weight')`, исторически в фунтах) конвертируется в килограммы (`*0.453592`) перед передачей в логику autothrottle; если `total_weight` отсутствует, fallback — 60000 (единицы не уточнены в исходной задаче — проверьте сами).
- Round 2 (REM-02, была часть CHANGES REQUIRED — раньше тесты были только math-only, не трогали реальный код): добавлены `test_calculate_approach_speeds_kwargs` и `test_calculate_approach_speeds_fallback_weight`, которые шпионят на реальный `AutoLandSystem._calculate_approach_speeds(config)` через `MagicMock`, проверяя `pytest.approx(8000/3.28084)` и `pytest.approx(132277*0.453592)`.

## Вопросы regression-sentinel (обязательны все четыре)

### Q1. Фикс закрыт по сути, а не косметически?
- Проверить все места в `modules/autothrottle.py` и `main.py`, где вес самолёта читается или используется — нет ли других веток кода (например, альтернативный расчёт тяги в интерфейсе SimConnect/WASM или в другом методе), которые всё ещё предполагают фунты или не применяют конверсию.
- Сверьте значение fallback (60000 кг или фунты?) с правдоподобным весом самолёта, для которого писалась система (см. `runway_length`/`approach_speed` в `ApproachConfig` для подсказки типа самолёта) — если fallback завышен/завишен в 5+ раз, это стоит отметить отдельно.
- Загляните в formula: перевод `total_weight` в килограммы происходит только один раз и нет двойной конверсии (то, что раньше было LITE-004 для throttle range — проверьте по аналогии нет ли таких же двойных конверсий в расчёте скоростей захода).

### Q2. Нет ли новых регрессий в коде, внесённых изменениями?
- Проверить, что исправление единиц веса не сломало другие вычисления, зависящие от `aircraft_weight_kg` (например, расчёт Vref/Vapp в `modules/autothrottle.py`, если он ожидал раньше вес в фунтах и где-то внутри есть своя константа/коэффициент, рассчитанный на фунты).
- Проверить границы: отрицательный/нулевой `total_weight`, строковое значение в telemetry — не приводит ли это к exception или к молчаливому применению неверного fallback.
- Проверить, не влияет ли ошибка в весе на safety_guard (например, если approach speed/Vref считается через aircraft_weight_kg и затем используется в G3/G4 как `vref` — ошибка в конверсии веса могла бы каскадно сдвинуть границы GROSS_UNDERSPEED/OVERSPEED).

### Q3. Нет ли регрессии/слепых зон в тестах?
- `test_calculate_approach_speeds_kwargs`/`test_calculate_approach_speeds_fallback_weight` спионят на реальный `_calculate_approach_speeds` — проверьте, действительно ли они вызывают метод реального `AutoLandSystem` (например через `AutoLandSystem.__new__`), а не копируют формулу отдельно в тесте (как это было в round 1, где тесты были math-only).
- Проверьте полноту ковера граничных случаев: total_weight отсутствует в telemetry вообще, `total_weight=0`, `total_weight` отрицательный/NaN/inf — какой из этих сценариев не покрыт.
- Проверьте, есть ли тесты на сам расчёт в `modules/autothrottle.py` (если он принимает вес в килограммах и что-то считает сам) — или проверка есть только на уровне `main.py`.

### Q4. Нет ли рассогласования между кодом/тестами/CI?
- Сравните реальную сигнатуру `_calculate_approach_speeds` с тем, как ее вызывают тест (какие аргументы реально передаются/мокаются).
- Проверьте, действительно ли `pytest.approx(8000/3.28084)` и `pytest.approx(132277*0.453592)` соответствуют реальным единицам в коде (футы→футы/м для высоты и фунты→кг для веса) или это числа скопированы из commit message без проверки соответствия коду.
- Фактически прогоните (описательно, без исполнения) логику по коду и сравните с assert-ами в тестах — нет ли рассогласования в единицах или формулах.

## Формат ответа
Отдельный отчёт `AUDIT-autothrottle-<sha>.md`, структура как в PASS A-LITE и в предыдущем аудите safety_guard:
- Файл/строка, описание находки, статус (STATIC_CONFIRMED/STATIC_SUSPECTED), связанный вопрос (Q1-Q4), короткая цитата кода.
- Отдельно — вердикт по каждому из четырёх вопросов: чисто / есть проблемы.
- Для каждой гипотезы о влиянии одного участка кода на другой (как Q2/Q3 про cascading эффекты) — приводите точный вызов/переменную, а не общее утверждение.
- Укажите точный SHA `master`, на котором проводили аудит.
