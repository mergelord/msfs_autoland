TASK: AUDIT-PRESIM — полное ревью+аудит всех модулей перед боевым тестированием в симе
Режим: строго read-only. Никаких правок кода, коммитов, веток. Только чтение и отчёт. Рабочая копия — C:\BAT\msfs_autoland, перед началом проверь: git status чистый (кроме TASKS/), HEAD = e2cc5b5 на master. Если нет — стоп и доклад.
Объём: все файлы modules/*.py, main.py, control.py и любые другие исполняемые модули проекта, плюс сверка с tests/. Ни один модуль не пропускать — в отчёте должна быть строка по каждому файлу, даже если находок нет («чисто»).
Фокус аудита — готовность к live-режиму в MSFS (не к тестам с фейками):
1. Контракты API — главный урок P1-1: сверь КАЖДЫЙ вызов методов control.py, adapter'ов и SimConnect-событий с фактической сигнатурой и семантикой (target value vs boolean, существование метода, единицы аргумента). Ищи аналоги бага set_airspeed_hold(False).
2. Fail-closed дисциплина — все обращения к telemetry-словарям (прямые ['key'] vs .get()), поведение при None/отсутствующих полях, отказах readback, потере SimConnect в каждой фазе захода. Что произойдёт при выпадении телеметрии на 244ft?
3. Порядок условий — аналоги P1-3 (LANDING/APPROACH shadowing): просмотри все if/elif-каскады по фазам, высотам, порогам на перехват более общим условием.
4. Единицы и пороги — футы/метры/узлы/fpm по всем границам вызовов; DH/MDA логика; sanity дефолтов конфига.
5. Время и такты — использование clock/monotonic, таймауты, устаревшая телеметрия, предположения о частоте тика, что случится при заморозке/лаге сима.
6. Исключения в основном цикле — любое место, где необработанный exception убьёт control loop в полёте.
7. Go-around пути — достижимость, идемпотентность, отсутствие тупиков после go-around.
8. Логирование — хватит ли логов для послеполётного разбора инцидента.
9. Дыры тестового покрытия — какие safety-инварианты не покрыты; предложи конкретные новые replay-сценарии (fixtures).
Формат отчёта — TASKS\REVIEWS\AUDIT-PRESIM-e2cc5b5.md:
- Шапка: commit SHA, дата, список проверенных файлов с LOC.
- Каждая находка: ID AUD-NN, severity P0 (нельзя лететь) / P1 (высокий риск) / P2 (желательно) / P3 (косметика), файл + номер строки + точная цитата кода, объяснение риска именно в live-режиме, предлагаемый фикс (текстом, не патчем), уверенность (high/medium/low).
- Каждую находку перепроверь по фактическому содержимому файла перед включением — не по памяти (урок false positive P1-2).
- Итог: сводная таблица по severity + вердикт GO / NO-GO для боевого теста с обоснованием.
Отчёт не коммитить (TASKS\ локальная). По завершении пришли файл отчёта целиком.