TASK: DEPGRAPH-3971ba1
Контекст: построение полного графа зависимостей проекта. Analysis-only — никаких изменений в коде, ничего не коммитить и не пушить. Артефакты складываются локально в research/depgraph/ (untracked).
Шаг 0 — бейзлайн: git rev-parse HEAD должен вернуть 3971ba12113d8994665b1c9a172f2dca6c9e3855. Если нет — STOP, сообщить фактический SHA.
Область: узлы графа = все 47 .py в modules/ (46 модулей + __init__.py) плюс main.py и gui.py — итого 48 файлов. Тесты (tests/) в граф НЕ включать, но отдельным списком выдать production-модули, которые не импортируются ни одним тестом.
Метод — строго AST, без regex:
Для каждого файла: ast.parse + ast.walk, собрать все Import / ImportFrom.
Оставить только рёбра на локальные модули проекта (modules/*, main, gui); stdlib и third-party отбросить, но посчитать их количество на файл.
Для каждого ребра фиксировать: {src, dst, lineno, lazy}, где lazy=true, если импорт находится внутри функции/метода (не на верхнем уровне модуля).
Динамические импорты (importlib, __import__) — отдельным списком с lineno, в основной граф не включать.
Анализ (networkx):
SCC + nx.simple_cycles (циклы длиной ≤6);
fan-in и fan-out топ-10;
топологические слои: L0 = утилиты без внутренних зависимостей → ... → main/gui наверху;
сироты (узлы без входящих И исходящих внутренних рёбер).
Артефакты в research/depgraph/:
depgraph.json — список рёбер {src, dst, lineno, lazy} + node_link_data графа;
depgraph.mmd — Mermaid flowchart TD: subgraph на каждый слой, рёбра циклов выделены стилем, lazy-рёбра пунктиром (-.->);
depgraph.png — рендер через graphviz (резервный канал, окружение уже готово);
DEPGRAPH-REPORT.md — сводка: счётчики (узлы/рёбра/lazy/динамические), циклы, топ fan-in/fan-out, слои, сироты, модули без тестовых импортов.
Критерии приёмки:
Покрыты все 48 файлов (при расхождении числа — STOP и сообщить список);
Только AST, никакого regex по тексту;
Каждое ребро воспроизводимо: src + lineno позволяют найти строку импорта;
Mermaid валиден (проверить парсером/превью перед сдачей);
В ответ вставить целиком: DEPGRAPH-REPORT.md, содержимое depgraph.mmd и счётчики.
Формат ответа: COMPLETED_NO_CHANGES + артефакты по п.5. Если что-то блокирует — STOPPED_ON_GATE + причина дословно.